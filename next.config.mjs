/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['photos.google.com'], // Add Unsplash domain to the list of allowed domains
    },
};

export default nextConfig;
